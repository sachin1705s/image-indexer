# Models

This folder is reserved for model notes, optional local model assets, and future adapters.

The MVP loads open-source models through the Python backend and stores downloaded weights in the local Hugging Face cache on first use.
